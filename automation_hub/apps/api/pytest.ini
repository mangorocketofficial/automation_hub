[pytest]
# 테스트 파일 경로
testpaths = tests

# Python 파일 패턴
python_files = test_*.py

# 테스트 함수 패턴
python_functions = test_*

# asyncio 모드
asyncio_mode = auto

# 출력 옵션
addopts = -v --tb=short

# 환경변수 (테스트용)
env =
    DEBUG=true
    SUPABASE_URL=https://test.supabase.co
    SUPABASE_KEY=test-key
